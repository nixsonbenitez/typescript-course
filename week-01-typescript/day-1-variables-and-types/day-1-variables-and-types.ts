// Week 1 · Day 1 — Variables & types
// Concepts: string, number, boolean, template strings, if/else on a boolean.

const yourName: string = "Daddy";
const daysPerWeek: number = 4;
const hasShipped: boolean = true;

console.log(`I'm ${yourName} and I code ${daysPerWeek} days per week.`);

if (hasShipped) {
  console.log(`You ${yourName}, you will receive a package for coding!`);
} else {
  console.log(`${yourName}, you will not receive a package. Lock in.`);
}

export{};